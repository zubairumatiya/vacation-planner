const ResetPasswordWait = () => {
  return (
    <h4>
      If an account with that email exists, we have sent a password reset link
      to it. The link will expire in 15 minutes, please check your email...
    </h4>
  );
};

export default ResetPasswordWait;
